export type SampleType = {
  title: string;
  label: string;
  type: string;
  audioUrl?: string;
  imageUrl?: string;
  contributor?: string;
  composer?: string;
  group?: string;
  primary?: string;
};

// export type SampleType
